<table>
	<tr>
		<th>Produs</th>
		<th>Cantitate</th>
		<th>Pret Unitar</th>
		<th>Pret Total</th>
	</tr>

	<% @cart.cart_items.each do |c| %>
		<tr>
			<td><%= c.product.name %></td>
		  	<td><%= c.quantity %></td>
			<td><%= c.product.price %></td>
			<td><%= c.total_price %></td>
		</tr>
	<% end %>
</table>
<hr />
<table>
	<tr><td>Total Cumparaturi</td>	<td><%= @cart.subtotal %></td></tr>
</table>